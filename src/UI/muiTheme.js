import { createMuiTheme } from 'material-ui/styles';

const muiTheme = createMuiTheme({
    overrides: {
      
    },  

    palette: {
      primary: {
        light: '#ffffff',
        main: '#f5f5f5',
        dark: '#c2c2c2',
      },
      secondary: {
        light: '#7a7cff',
        main: '#304ffe',
        dark: '#0026ca',
      },
    },
  });

  export default muiTheme;