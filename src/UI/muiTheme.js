import { createMuiTheme } from 'material-ui/styles';

const muiTheme = createMuiTheme({
    palette: {
      primary: {
        light: '#ffffff',
        main: '#f5f5f5',
        dark: '#c2c2c24',
      },
      secondary: {
        light: '#75a7ff',
        main: '#2979ff',
        dark: '#004ecb',
      },
    },
  });

  export default muiTheme;